[package]
name = "wordbrain-resource-manager"
version = "0.1.0"
edition = "2021"

[dependencies]
aws-config = "1.8"
aws-credential-types = "1.2"
aws-sdk-s3 = { version = "1.128.0", features = ["behavior-version-latest"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2"
